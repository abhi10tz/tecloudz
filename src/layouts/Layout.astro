---
import { ClientRouter } from 'astro:transitions';
import type { ImageMetadata } from 'astro';

interface Props {
  title: string;
  description?: string;
  image?: ImageMetadata | string;
}

const { title, description = "Cutting-edge tech stories that matter. Privacy-first news, tutorials, and guides.", image } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);

const resolveImageURL = (img: ImageMetadata | string | undefined) => {
  if (!img) return new URL('/favicon.svg', Astro.site);
  if (typeof img === 'string') return new URL(img, Astro.site); // Handle string paths
  return new URL(img.src, Astro.site); // Handle ImageMetadata
};

const socialImageURL = resolveImageURL(image);
---

<!doctype html>
<html lang="en">
  <head>
    <ClientRouter />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <!-- Canonical URL -->
    <link rel="canonical" href={canonicalURL} />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={socialImageURL} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={socialImageURL} />

    <style is:global>
      /* --- 1. Define Variables (Light Mode Default) --- */
      :root {
        --bg-color: #ffffff;
        --text-main: #111111;
        --text-muted: #666666;
        --border-color: #eaeaea;
        --accent: #000000;
        --hover-bg: #f5f5f5;
      }

      /* --- 2. Dark Mode Override --- */
      @media (prefers-color-scheme: dark) {
        :root {
          --bg-color: #111111;
          --text-main: #e0e0e0;
          --text-muted: #a0a0a0;
          --border-color: #333333;
          --accent: #ffffff;
          --hover-bg: #222222;
        }
      }

      /* --- 3. Global Styles --- */
      html { 
        font-family: system-ui, sans-serif; 
        background: var(--bg-color); 
        color: var(--text-main); 
      }
      body { margin: 0; padding: 0; display: flex; flex-direction: column; min-height: 100vh; }
      a { text-decoration: none; color: inherit; }
      a:hover { color: var(--accent); }

      /* --- 4. Global Footer Styles --- */
      .minimal-footer {
        margin-top: auto;      /* Pushes footer to the bottom */
        margin-left: auto;     /* CENTERS it horizontally */
        margin-right: auto;    /* CENTERS it horizontally */
        width: 100%;
        max-width: 720px;
        
        padding: 2rem 1.5rem;  /* Adds side padding to match your Main content */
        text-align: center;
        border-top: 1px solid var(--border-color);
        font-size: 0.8rem;
        color: var(--text-muted);
      }

      .footer-links {
        margin-bottom: 1rem;
        display: flex;
        justify-content: center;
        gap: 1.5rem;
      }
      
      .footer-links a {
        color: var(--text-muted);
        font-weight: 500;
      }
      
      .footer-links a:hover {
        color: var(--accent);
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <slot />

    <footer class="minimal-footer">
      <div class="footer-links">
        <a href="/privacy">Privacy Policy</a>
        <a href="/terms">Terms</a> <a href="/about">About</a>
        <a href="/posts">Posts</a>
      </div>
      <p>&copy; 2025 tecloudz</p>
    </footer>
  </body>
</html>